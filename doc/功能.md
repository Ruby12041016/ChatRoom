ChatRoom 
├── build/                  编译输出目录（外部编译，不污染源码）
├── doc/                    文档集中存放
|
├── CMakeLists.txt          项目构建配置（CMake 必做要求）
├── json.hpp                第三方 JSON 序列化/反序列化库
│
|
|── global.h                全局定义：消息类型枚举、协议格式、错误码、通用头引用
│── logger.h                服务器日志封装（基于 glog，满足日志要求）
│── utils.h / utils.cc      通用工具：密码加密、参数校验、时间工具
│
|
│── mysql_pool.h / mysql_pool.cc  MySQL 连接池，持久化存储核心数据
│── redis_pool.h / redis_pool.cc  Redis 连接池，仅存在线状态/离线消息/验证码
│
|
│── server.cc         服务端：Epoll 多路复用 + timerfd 心跳 + 连接管理 + 粘包处理
│── client.cc         客户端：TCP 连接 + CLI 交互 + 消息收发 + 指定 IP:Port
│ 
│── message.h / message.cc    消息解析、按类型路由到对应业务模块
│
├── account.h / account.cc    账号系统：注册/登录/注销 + 密码加密
├── friend.h / friend.cc      好友系统：增删查/在线状态/权限校验/私聊
├── group.h / group.cc        群组系统：群管理/权限校验/群聊广播
├── history.h / history.cc    消息系统：历史记录/离线消息/上线推送
|── router.h/router.cc


